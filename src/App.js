import React from 'react';
import { AppRouter } from './routers/AppRouter';

export const App = () => {
    return (
        <div className="container">
            <AppRouter />
        </div>
    )
}